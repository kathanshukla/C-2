// Queue Lista ligada FIFO

Typedef struct Node{
	int data;
	Node * next;

} *Nodo;

Typedef struct Queue;

Nodo inicia(){
	Temp * = malloc(sizeof(struct Node));
	Temp->first = Temp->last = NULL;
	return Temp;
}

Void enqueue(){
	
}